//! The Joy Capture Agent daemon.
//!
//! `joy-agentd` is the binary that wires every crate into a running agent —
//! capture, analysis, media, control, provisioning, extensions, and storage —
//! under a supervisor that owns each stage's lifecycle. It also hosts the CLI
//! and daemon entry point.
//!
//! This is currently a scaffold entry point; wiring lands as the individual
//! subsystems come online.

fn main() {
    println!("joy-agentd: not yet implemented");
}
